const numbers = [1, 2, 3, 4, 5];

numbers.forEach((Item, index, arr) => {
    console.log('Index[' + index + '] = ' + Item + arr)
    console.log(arr)

});

// function outputItem(Item, index, arr) {
//     console.log('a[' + index + '] = ' + Item)

// }